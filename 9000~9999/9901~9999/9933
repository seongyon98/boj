n = int(input())
words = []
for _ in range(n):
    word = input()
    words.append(word)
    
for word in words:
    reversed_word = word[::-1]
    if reversed_word in words:
        p = word
        break

print(len(p), p[len(p)//2])
