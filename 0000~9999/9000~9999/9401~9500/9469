import sys
input = sys.stdin.readline

p = int(input().rstrip())

for _ in range(p):
    n, d, a, b, f = map(float, input().split())
    n = int(n)
    t = d / (a+b)
    result = f * t
    
    print(n, "{:.6f}".format(result))