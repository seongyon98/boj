N = int(input())

total = 0
for _ in range(N):
    h, v = map(int, input().split())
    if h == 136:
        total += 1000
    elif h == 142:
        total += 5000
    elif h == 148:
        total += 10000
    else:
        total += 50000

print(total)
