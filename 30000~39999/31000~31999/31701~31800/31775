def check_global_phrase(s1, s2, s3):
    first_chars = {s1[0], s2[0], s3[0]}
    
    if first_chars == {'l', 'k', 'p'}:
        return 'GLOBAL'
    else:
        return 'PONIX'
    
s1 = input().strip()
s2 = input().strip()
s3 = input().strip()

print(check_global_phrase(s1, s2, s3))