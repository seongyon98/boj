def count_claps(N):
    total = 0
    for num in range(1, N + 1):
        total += str(num).count('3') + str(num).count('6') + str(num).count('9')
    
    return total

N = int(input())
print(count_claps(N))
